## Summary
- Create MainModal.tsx file with 68 additions and no deletions.
- AliSubhani1, branch head → ali/n8n-changes, number of files/commits: 1/1.
- No top risks identified.

## Technical Details
The pull request introduces a new `MainModal` component in `MainModal.tsx`, added through 68 new additions with no deletions. This file is the only modification made to the codebase as part of this PR, indicating a focused, self-contained addition. The changes involve importing necessary dependencies from React and other modules, defining props for the modal component, and implementing its functionality using React hooks.

## Implementation Explanation
This change introduces modularity by adding a separate `MainModal` component that encapsulates modal functionality. By doing so, the code adheres to principles of Separation of Concerns (SoC) and maintains reusability. The addition is significant because it introduces new interactivity to the application, governed by React hooks for state management.

## Impact Analysis
The introduction of `MainModal` affects various components within the system that may use or interact with this modal. Downstream effects might include changes in layout adjustment logic due to the height checks implemented using `window.innerHeight`. The blast radius is localized mainly to components that directly or indirectly import and utilize the features provided by `MainModal`.

## Risk & Mitigation
A notable risk is related to potential edge cases not accounted for with the current implementation. Specifically, the solution relies on window dimensions for determining display layout. However, it lacks robust handling of non-standard height metrics (e.g., mobile devices) and might encounter issues in responsive design scenarios where such assumptions are invalid.

## Detailed Changes (per file)
### **MainModal.tsx** — *added*, +68/-0*.
Create a new MainModal component with React hooks for modal functionality and styling.

```diff
@@ -0,0 +1,68 @@
+import React, { useEffect, useState } from 'react';
+import CancelIcon from '../Assets/CrossIcon.svg';

+type ModalProps = {
+  title: string;
+  setIsModalOpen: React.Dispatch<React.SetStateAction<boolean>>;
+  setErrorMessage?: React.Dispatch<React.SetStateAction<string>>;
+
+  children?: React.ReactNode;
+};

+const Modal: React.FC<ModalProps> = ({ title, setIsModalOpen, setErrorMessage, children }) => {
+  const [isTallScreen, setIsTallScreen] = useState(window.innerHeight > 800);

+  useEffect(() => {
+    const handleResize = () => {
+      setIsTallScreen(window.innerHeight > 700);
+    };
+    window.addEventListener('resize', handleResize);
+    return () => {
+      window.removeEventListener('resize', handleResize);
+    };
+  }, []);

+  const handleModalClose = () => {
+    if (setErrorMessage) setErrorMessage('');
+    setIsModalOpen(false);
+  };

+  return (
+    <>
      <div className="fixed inset-0 z-10 bg-gray-1 opacity-30"></div>
      <div
        className={`fixed z-10 inset-0 overflow-y-auto backdrop-blur-sm flex justify-center ${isTallScreen ? 'items-center' : 'items-start'}`}
      >
```

## Images changed
No images were changed in this pull request.

## Discussion signals
- Issue comments: 0
- Reviews: 0
- Inline review comments: 0

## Next Steps
No action needed.