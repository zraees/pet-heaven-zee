Here is your refined Markdown document:

## Summary
- Add ExtendedModel component for modal functionality, 1 file/1 commit.
- Author: zraees, branch head → main.

## Technical Details  
- This PR introduces a new TypeScript file (`src/Components/ExtendedModel.tsx`) to extend model functionality. 
- The `Modal` component is defined with props for title, setting the modal open state, and an optional error message.
- It includes event handling for window resize and modal close.

## Implementation Explanation  
- The changes enable enhanced modal functionality by introducing a new React component (`ExtendedModel`) that encapsulates modal-related logic. 
- This implementation provides flexibility in terms of modal size adaptation (tall screens vs regular screens) through state management using `useState`.
- It enables event-driven close of the modal and resizing handling for optimal layout, leveraging `window.addEventListener` and cleanup with `useEffect`.

## Impact Analysis  
- The updated code affects the modular structure by adding a new React component (`ExtendedModel.tsx`).
- The impact on system behavior is an enhanced modal functionality, with dynamic size adaptation and optimal layout.
- This PR introduces minor changes to the file structure but expands usability with additional features.

## Risk & Mitigation  
- No notable risks or regressions are directly associated with this implementation. 
- Code organization and component encapsulation facilitate maintainable code without increased complexity or performance bottlenecks.
- The change adheres closely to best practices for clean, modular, and reusably designed components.
- Integration and functional testing should verify behavior consistency across the application.

## Detailed Changes (per file)
### **src/Components/ExtendedModel.tsx** — *added*, +68/-0*.
This PR adds a new TypeScript file to extend model functionality. The `Modal` component is defined with props for title, set modal open, and optional error message. It includes event handling for window resize and modal close.

```diff
@@ -0,0 +1,68 @@
+import React, { useEffect, useState } from 'react';
+import CancelIcon from '../Assets/CrossIcon.svg';

+type ModalProps = {
+  title: string;
+  setIsModalOpen: React.Dispatch<React.SetStateAction<boolean>>;
+  setErrorMessage?: React.Dispatch<React.SetStateAction<string>>;
+
+  children?: React.ReactNode;
+};

+const Modal: React.FC<ModalProps> = ({ title, setIsModalOpen, setErrorMessage, children }) => {
+  const [isTallScreen, setIsTallScreen] = useState(window.innerHeight > 800);

+  useEffect(() => {
+    const handleResize = () => {
+      setIsTallScreen(window.innerHeight > 700);
+    };
+    window.addEventListener('resize', handleResize);
+    return () => {
+      window.removeEventListener('resize', handleResize);
+    };
+  }, []);

+  const handleModalClose = () => {
+    if (setErrorMessage) setErrorMessage('');
+    setIsModalOpen(false);
+  };

+  return (
+    <>
      <div className="fixed inset-0 z-10 bg-gray-1 opacity-30"></div>
      <div
        className={`fixed z-10 inset-0 overflow-y-auto backdrop-blur-sm flex justify-center ${isTallScreen ? 'items-center' : 'items-start'}`}
      >
```

## Images changed
No images were modified in this PR.

## Discussion signals
- Issue comments: 0
- Reviews: 0
- Inline review comments: 0

## Next Steps
Review the new `ExtendedModel` component for modal functionality.